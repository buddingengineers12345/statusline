# CLAUDE.md

**Shared knowledge lives in @AGENTS.md — read it first.**

This file holds only Claude-specific notes.

## Claude-specific notes

- Statusline scripts are local (no API tokens). Prefer `grid.py` or `grid.sh` against `sample.json` when verifying layout.
- Wire via Claude Code `statusLine.command` in `~/.claude/settings.json` — point at this repo's absolute script path.
- Do not confuse this repo with the agentspace workspace root; AGENTS.md here is statusline-only.
